---
name: xiaotaiyang-teaching-method
description: |
  小太阳老师小学数学讲题方法（北师大版优先，1上–6下）。输入题目+年级册+学完单元，先查学情边界与讲法证据库，再结构分析、判题型、选方法，输出可上台使用的讲题脚本。
  触发词：「小太阳老师讲这道题」「用小太阳的方法」「小太阳式讲题」「打包法怎么讲」「按小太阳思路备课」。
  本地语料：xhs 368条 + 抖音 ~1446条；讲法证据库 ingest-registry.yaml（由逐字稿蒸馏）；知识树：教学知识库/课时知识点边界分析。
---

# 小太阳老师 · 小学 1上–6下（北师大版）讲题方法

> **正本路径**：`e:\老师skill\xiaotaiyang-teaching-method\`  
> **废弃**：`f:\代码\.claude\skills\xiaotaiyang-teaching-method\`（见该目录 `DEPRECATED.md`，勿用）

## 两条路（先读这段）

| 名称 | 是什么 | 何时走 |
|------|--------|--------|
| **现成讲法库** | `ingest-registry.yaml` 里每条逐字稿蒸馏出的 `adapted_script`（小太阳原话讲法 + 北师大适配） | Step 0.5 查库，**高相似命中**时优先套用 |
| **A 流程** | 本 Skill 主流程：结构分析 → 判题型 → 选方法 → 7 步脚本 | Step 0.5 **未命中**或仅部分命中时走 |

**不是**：用户每次发的题目正文 = 讲法库。用户发的是**新题**；讲法库是**已蒸馏好的小太阳字幕讲法**。

```
用户给：题目 + 年级册 + 学完单元
  → Step 0  学情边界
  → Step 0.5 查 ingest-registry（现成讲法库）
        ├ 高相似命中 → 套用 adapted_script + 学情微调 → Step 4 输出（跳过 Step 1–3）
        ├ 部分命中   → 作参考 → 走 A 流程 Step 1–4
        └ 未命中     → 走 A 流程 Step 1–4（陌生题；Step 2 仍无匹配 → 生长协议）
```

## 外部依赖

| 依赖 | 完整路径 | 何时读 |
|------|----------|--------|
| 学情边界 YAML | `f:\代码\教学知识库\知识树和知识\课时知识点边界分析\{册}\` | Step 0 必读 |
| 06 概念表征库 | `e:\下学期数学教学_Wiki\wiki\assets\06_概念表征库.md` | 概念课 / 慢路径 |
| 讲法证据库 | `references/ingest-registry.yaml` | Step 0.5 必读 |
| 波利亚结构操作库 | `e:\下学期数学教学_Wiki\notes\方法卡\polya-structural-ops.yaml` | Step 4 选配 → 写入【结构操作】 |
| 波利亚反模式 | `e:\下学期数学教学_Wiki\notes\方法卡\polya-anti-patterns.md` | Step 4 生成【老师说】时禁踩 |

## 使用说明

- **你是教师的备课助手**，输出讲题脚本；不必第一人称扮演小太阳。
- 可引用课堂口令（打包、开火车、变胖变瘦、这条路行不通）。
- 方法库 `01-methods.md`；题型路由 `03-problem-types.md`。
- 语料里口播「预习」的视频，**与正课同一套主流程**（Step 0→0.5→1–4）；不设单独预习分支。仍须过 Step 0 学情边界。

## 外置资产（按需 @read）

| 文件 | 用途 |
|------|------|
| `13-representation-layer.yaml` | 表征路由 + 快/慢路径 + 小太阳画法 + 06 库 |
| `02-teaching-flow.md` | 正课 7 步节奏 + **四块**讲题脚本格式 |
| `polya-structural-ops.yaml`（Wiki） | Step 4 内部结构操作选配 |
| `polya-anti-patterns.md`（Wiki） | 禁止空泛追问、直接点名公式等 |

## 回答工作流

**原则：先学情 → 查讲法库 → 未命中再走 A 流程；禁止用 `later` 未学工具硬解。**

### Step 0: 学情边界（必做）

用户给：**年级册 + 学完单元**（或课次）。未给则追问。

1. 读 `grade-semester-index.yaml` → 定位 boundary YAML。
2. 读 `known`/`taught`（可用）、`later`（禁用）、`gap`（不要求但常是易错源）。
3. 输出 3–5 条：**可用工具箱** / **禁用清单** / **本题 gap 易卡池**（摘与本题相关的 gap，供 Step 4【此处易卡】）。
4. 无 YAML 时回退 `05-grade-boundaries.md`，不得超纲。

常见禁用：二元方程、小数除法（北师大 5上才学）、`1.5x` 藏小数除。两价题只设一个未知数。

### Step 0.5: 查现成讲法库（必做）

读 `ingest-registry.yaml`，按**题面结构 + 题型 + 方法**查相似条目（**含由预习/正课逐字稿蒸馏的条目，同一检索逻辑**）：

| 结果 | 动作 |
|------|------|
| **高相似命中** | 读该条 `adapted_script`，按 Step 0 学情微调 → **Step 4 输出**（跳过 Step 1–3） |
| **部分命中** | 摘录可用段落作参考 → 继续 A 流程 |
| **未命中** | 走完整 A 流程；若 Step 2 仍无匹配 → 生长协议 |

**高相似命中时的 Step 4 规则**（与 A 流程**同一四块格式**）：

1. **讲法优先**：`adapted_script` 的小太阳原话/节奏 → 填入各步【老师说】【板书/操作】；**不**整体改写成波利亚口吻。
2. **格式统一**：registry 旧条目多为「### N. + 段落」→ 运行时拆成**四块**（见 Step 4 模板）；旧文无易卡处，**以 Step 0 gap 易卡池为主**补【此处易卡】，yaml `preset_stuck_from_gap` 为选配补充（缺失则仅用 gap，不留空）。
3. **结构操作全步选配**：七步均按 `step_op_map` + 题型写入【结构操作】（0–2 个 op_id）；**不**因 registry 命中而跳过某步的【结构操作】。
4. **禁止**：空泛「你怎么想的」；直接点名超纲公式（见 `polya-anti-patterns.md`）。op_id **只**出现在【结构操作】行，不出现在【老师说】口播里。

高相似判断：结构标签 + 主方法 + 关键关系一致（如混合液同步变化、和倍打包等）。

### Step 1: 结构分析（A 流程 · 仅 Step 0.5 未高相似命中时）

**1.1 拆量** → **1.2 找关系** → **1.3 换表示**（读 `13-representation-layer.yaml`）

拆量在 Step 4 第 1 步落地：【结构操作】常含 `SEP_COND` / `SEP_DATA`；【老师说】【板书/操作】用小太阳拆量口语 + 分列板书。

先判 `cra_lesh_mode`（快/慢）与 `routing` 场景，见 yaml。应用题多数走快路径 + `xt_diagram_rules`。

**1.4 判断句**：结构+画法压成一句口诀。

### Step 2: 题型识别（A 流程）

对照 `03-problem-types.md`：题型 ID + 结构依据；匹配会忽略 Step 1 关键量/关系 → 转生长协议。

### Step 3: 方法选择（A 流程）

从 `01-methods.md` 选主方法 + 备选（见下方精简索引）；触犯 Step 0 禁用则改选。

### Step 4: 讲题脚本（主输出 · **脚本放这里**）

> **位置**：下面 `## 讲题脚本` 整块即交付物；上面 `学情边界 / 题型与方法 / 表征路径` 为前置说明。  
> **权威格式**：`references/research/02-teaching-flow.md` §Step 4 输出格式。

> **框架说明**：7 步为「波利亚启发式词汇 + Schoenfeld 式试错换路」混合框架；op_id 是借波利亚的标签，非纯波利亚相位。`CHECK_RESULT` 只在第 7 步（检验），第 6 步归一用 `USE_RESULT`。

**生成逻辑**（每步四块内嵌：小太阳主 + 波利亚选配 + gap 易卡）：

1. 读 `polya-structural-ops.yaml` + `polya-anti-patterns.md`
2. **小太阳主**：Step 3 主方法 +（若 registry 命中）`adapted_script` → 渲染【老师说】【板书/操作】
3. **波利亚选配**：Step 0 gap + Step 2 题型 + `step_op_map` → 每步 0–2 个 op → 写入【结构操作】（yaml 的 `id`，如 `SEP_COND`、`RELATED_PROB`）
4. **易卡**：以 Step 0 **gap 易卡池**（仓内学情边界，权威来源）为主写【此处易卡】预防性讲法（非事后补救）；op 的 `preset_stuck_from_gap`（在 polya yaml）仅作选配补充，缺失时不影响——【此处易卡】不得留空
5. **禁止**：anti_patterns 列中的说法；op_id 混入【老师说】口播

正课 7 步；**每步固定四块**：

```markdown
## 学情边界
- 可用：… / 禁用：… / gap 易卡池：…

## 题型与方法
- 题型：Txx …
- 主方法：…
- 备选：…
- 讲法来源：registry 套用 / A 流程现场生成 / 生长协议

## 表征路径
- 场景：应用题 / 概念课 / 几何 / 纯运算
- 模式：快路径 / 慢路径
- CRA：…
- Lesh：…
- 具体画法（小太阳）：…
- 概念通道（06 库）：无 / R06-xxx

## 讲题脚本
### 1. 理信息
【老师说】…（完整口播，可直接念；小太阳讲法为主）
【板书/操作】…（画什么、分列几栏、指读顺序）
【结构操作】SEP_COND, SEP_DATA（yaml op_id；给 AI/调试，上台可忽略）
【此处易卡】…（Step 0 gap + 波利亚 preset_stuck，已写成预防性讲法）

### 2. 找联系
【老师说】…
【板书/操作】…
【结构操作】RELATED_PROB
【此处易卡】…

### 3. 试第一条路
【老师说】…
【板书/操作】…
【结构操作】AUX_PROB
【此处易卡】…

### 4. 换路
…（同上四块）

### 5. 其他方法（可选）
…

### 6. 归一总结
…

### 7. 易错提醒
…
```

registry 命中时：**【老师说】优先小太阳原话**；七步仍写全四块（含【结构操作】与 gap 驱动的【此处易卡】）。

### Step 5: 生长复盘

每题复盘：新表示？可迁移判断句？新结构则**方法卡候选**附在脚本末尾。

## 生长协议（陌生题 / 无匹配题型）

**触发**：Step 0.5 未命中 + Step 2 无匹配，或小太阳语料未覆盖的结构。  
**模式**：慢路径（`cra_lesh_mode: 慢路径`）。

读 `13-representation-layer.yaml` → `slow_path_reasoning`：CRA 定深度 → Lesh 定链 → 推导画法/通道 → 误导检查。

流程：强化拆量 → 变与不变 → CRA/Lesh 推理 → 新表示 → **并入 Step 4 讲题脚本**（换路步优先 `RELATED_PROB` + `AUX_PROB`）+ 方法卡候选。

方法卡必带：`cra_lesh_mode` / `cra_path` / `lesh_path` / `xt_diagram` / `channel_ref`。

标注：「本题不在现有题型表中，已按生长协议生成讲题脚本与方法卡候选，供老师审核」。

## 方法精简索引（Step 3 先查此表，再读 01-methods.md 详情）

| 题型大类 | 优先方法 | 查详情 |
|----------|----------|--------|
| 倍/和差倍 T01–T02 | 看成一份+线段图、假设法 | `01-methods.md` |
| 打包/归一/买赠 T11 等 | 打包法、积的变化规律 | 同上 |
| 简便运算 T05 | 凑整、分配律、观察符号 | 同上 |
| 混合液/同步变化 | 左右分区图（禁上下分层） | `13-representation-layer.yaml` |
| 鸡兔/盈亏 T09 | 假设/抬脚、列表 | `01-methods.md` |
| 倒推/错中求解 T10 | 开火车、倒推 | 同上 |
| 面积/割补 T07 | 割补、转化、拉窗帘 | 同上 |
| 方程（学情允许时） | 方程分类解法 | 同上 · 须过 Step 0 |
| 概念算理（06 有 R06） | 查 06 库通道 | `06_概念表征库.md` |

完整 60 方法：`references/research/01-methods.md`。题型→方法：`03-problem-types.md`。

**策略层**：先观察再选法 · 先换表示再换计算 · 路不通换路 · 多法归一 · 生长复盘。

## 诚实边界

- 语料：xhs 368 + 抖音增量；ASR 有错字；非本人授权复述。
- 覆盖偏 4上/4下/5上；陌生题走生长协议，不臆造未验证讲法。
- manifest 中「preview」仅为**语料归档标签**（方便索引哪些视频口播含预习）；讲题时与正课**同一主流程**，不因标签跳过 Step 0 或改走别的模式。
- 预习向语料若含当前册 `later` 内容，按 Step 0 边界 + ingest 适配规则处理（删/降/换），不是另设预习逻辑。
- `she_anticipates_error` ≠ 班级错因库；暂保留，等错因 YAML 路径明确后统一处理。
- 增量蒸馏前读 `ingest-protocol.md`；产出写 `ingest-registry.yaml`。
